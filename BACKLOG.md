# Backlog

## Próximos

- plugin: **redesign da camada doutrinal** — consolidar 45 ADRs em ~10-12 temáticos sob hierarquia invertida pós-v2.14.0 + condensar `philosophy.md` (princípios + mapping operacional + ≥3 pattern como condição geral de YAGNI terminar; cortar § Codificação estrutural overhead) + codificar **política de admissão going forward** (*"isto é decisão reversível ou entendimento estabilizado?"*) como mecanismo de prevenção de re-acúmulo. Inversão do ônus da prova aceita: estrutura é empiricamente desordenada (acumulada via refinamento incremental, não desenhada); 3 princípios aplicados ao próprio inventário endossam consolidação (**Verdade** nomeia desordem; **Excelência** aplica padrão de mecanismo à doutrina; **Ockham** — versão enxuta carrega mesma carga). Precedente empírico no projeto: Onda 3 cluster index Addenda em ADR-005/-017 são provas de conceito do mesmo movimento aplicado a 2 clusters. Arco completo da conversa que produziu a direção + sketch da estrutura-alvo (11 ADRs) + **anti-regression checklist** da carga doutrinal que NÃO pode ser perdida + intent da prevenção + 8 riscos identificados registrados em `.claude/local/NOTES.md` entry 2026-05-30T06:08:04Z — `/triage` do charter consome como informational. Decomposição prevista: Onda A (charter + meta-ADR codificando redesign + política de admissão) → Onda B (`philosophy.md` condensado) → Ondas C-X (migração agrupada por cluster com design-reviewer validando cada absorção) → Onda final (admission policy enforcement em `/new-adr` + archive de ADRs antigos com redirects + propagação cross-refs). Bump major v3.0.0 esperado. Dogfood end-to-end (`/triage`, `/new-adr`, `/run-plan`, design-reviewer auto-fire, `/note`). **Atualização 2026-05-31:** Ondas A+B+C+D+E+F+G+H + ADR-052 meta-pattern shipped. **Onda A** — commits `bbf4430` (umbrella) + `715f455` (charter + ADR-045) + PR #88 (Bloco 1: CLAUDE.md bullet, merge `91d3f70`); 11/12 findings absorvidos. **Onda B** — commit `a09a16c` (plano) + PR #89 (Bloco 1: 5 ops em philosophy.md, merge `f758c8d`); 5 findings absorvidos incluindo F3 (V→O→E alternativa d abstrata); bifurcação (γ) dissolução plugin/consumer materializada via philosophy.md. **Onda C** — primeira migração cluster (cutucadas) — commit `2b147ca` (plano + ADR-046) + PR #90 (4 blocos: archive ADR-017+ADR-029 + archive/README.md + cross-refs em CLAUDE.md + procedure + 3 SKILLs, merge `dbac4d6`); 9 findings absorvidos pelo design-reviewer no /new-adr (F1 link rot em 2 categorias; F2 header redirect blockquote canonical; F3 archive index incremental; F4 cond 5 primária isolada [cond 4 NÃO aplica]; F9 fronteira ADR vs procedure per ADR-024). Pattern de migração validado empíricamente para ondas D-X. **Onda D** — segunda migração cluster (modo local; **primeira sem procedure file**) — commit `1500c17` (plano + ADR-047) + PR #91 (4 blocos: archive ADR-005+018+025+030 + archive/README.md ext + cross-refs em CLAUDE.md + 6 SKILLs + install.md + README.md; ~58 ocorrências em 9 docs vivos, merge `cd0b533`); 5 findings absorvidos pelo design-reviewer no /new-adr (F1 imprecisão numérica corrigida via grep empírico; F2 assimetria de trigger absorvida no § Decisão (b); F3 implementação history delegada ao archive [cutucada — opção (a)]; F4 cond 2 refinada "absorção consolidatória vs revogação"; F5 citações circulares); 1 finding caminho-único no Bloco 3 (path relativo `../../` em new-adr/SKILL.md). Pattern transferibilidade validada fora de cutucadas — F4 lesson reaplicada literal (cond 5 isolada). **Onda E** — terceira migração cluster (reviewers/curadoria; **calibração descendente** 2 ADRs vs 4 da Onda D) — commit `bf55199` (plano + ADR-048) + PR #92 (3 blocos: archive ADR-021+044 + archive/README.md ext + design-reviewer.md reformulação narrativa + 5 docs vivos; ~11 ocorrências em 6 docs vivos, merge `2858005`); 3 findings absorvidos caminho-único pelo design-reviewer no /new-adr (B1 ruído editorial rebatimento ADR-046; B2 § Origem histórica truncada removendo redundância com § Contexto; B3 § Decisão (d) parágrafo "pattern auto-consistente" removido — redundante com § Razões + § Trade-offs); 1 finding rejeitado caminho-único no Bloco 2 (convenção `§ Decisão (X)` já estabelecida em ADR-047 + plano Onda D + ADR-048 § Auto-aplicação). Sequência C→D→E exerce 2→4→2 ADRs (scope variado com vs sem procedure file). **Calibração emergente (Onda C):** target realista 13-15 consolidados — cutucadas saiu standalone vs sub-cluster de alinhamento; refinamento editorial do charter per ADR-045 fronteira "ajuste editorial vs revisão". Saldo inventário pós-Onda E: **40 ADRs vigentes** (drop líquido de 1 nesta onda; calibração descendente confirmada). **Onda F** — quarta migração cluster (execução/run-plan; **primeira com refinamento editorial documentado**) — commit `154a4f0` (plano + ADR-049) + PR #93 (3 blocos: archive ADR-004+028+039+041 + archive/README.md ext + design-reviewer/CLAUDE/triage/template/run-plan cross-refs; ~19 ocorrências em 5 docs vivos com hot spot 9 em skills/run-plan/SKILL.md, merge `f55e2a4`); 0 findings absorvíveis pré-commit do /new-adr (nano-flag editorial absorvido inline per ADR-047 pattern); 0 findings do doc-reviewer em Blocos 2+3 (sinal de saúde verde). **Refinamento editorial primeira instância documentado:** sketch original listava 5 ADRs (004+028+037+039+041); Onda F absorve 4 — ADR-037 EXCLUÍDO (pertence semanticamente a cluster discoverability/branding) + ADR-010 NÃO ABSORVIDO (decisão base de ADR-039 mas categoria distinta progress display com potencial consumers futuros). Pattern editorial para ondas G-X codificado em ADR-049 § Refinamento editorial. **Pendência de validação registrada:** Bloco 1 commitado sem invocação do doc-reviewer (violação de doutrina explícita "Não pular revisor"); mitigação pós-fato via Read manual + critérios 2-3 do plano; aderir estrito em ondas G-X. Saldo inventário pós-Onda F: **37 ADRs vigentes** (drop líquido de 3 nesta onda; paralelo a Onda D). **Onda G** — quinta migração cluster (componentes plugin; **scope máximo até hoje** 6 ADRs vs 4 em D+F; 2 em C+E) — commit `d298ced` (plano + ADR-050) + PR #94 (3 blocos: archive ADR-008+013+015+016+023+040 + archive/README.md ext + CLAUDE.md/philosophy.md cross-refs hot spot + install.md/skills/gen-tests/design-reviewer/2 hooks docstrings; 9 ocorrências em 7 docs vivos com hot spot 3 em CLAUDE.md bullets meta-doutrinais, merge `376a755`); 7 findings absorvidos caminho-único pelo design-reviewer no /new-adr (F1 ADR-019 absorção enviesada corrigida; F2 tabela retroativa 9 skills reproduzida literal; F5 pseudo-categoria "absorção consolidatória" removida; F6 verificações 9.bis+10.bis adicionadas; F7 ambiguidade temporal removida; F8 instrução data dinâmica; F3 retirado pelo reviewer) + 1 cutucada (F4 bidirecionalidade como meta-pattern editorial) absorvida via opção (a) — meta-pattern vive no charter post-merge, não no ADR temático; 1 finding caminho-único no Bloco 3 (sub-ref categoricamente imprecisa removida); 1 captura §3.5 materializada (cobertura ausente warning conservador — edit docstring-only; AST parse OK nos 3 hooks confirma comportamento intacto). **Refinamento editorial segunda instância (Onda G por inclusão vs Onda F por exclusão):** ADR-015 INCLUÍDO ao cluster apesar de omitido no sketch (5 ADRs originalmente) — cabe em ADR-045 § Decisão linha 56 fronteira "absorção em consolidado diferente do sketch". Pendência operacional Onda F endereçada: 4/4 blocos com doc-reviewer obrigatório invocado. Saldo inventário pós-Onda G: **32 ADRs vigentes** (drop líquido de 5 — maior até hoje; alinha com scope máximo). **Onda H** — sexta migração cluster (convenções editoriais; **3 ADRs absorvidos** vs 4 do sketch por constraint mecânico) — commit `f0dbcb2` (plano + ADR-051) + PR #95 (3 blocos: archive ADR-007+012+024 + archive/README.md ext + 4 procedures/README cross-refs hot spot user-facing + philosophy/skills/CLAUDE.md cross-refs; 12 cross-refs em 9 docs vivos vs 8 estimados — 4 gaps adicionais descobertos no Bloco 3 via F1 reviewer + grep final, merge `7991fdb`); **ADR-034 preservado vigente** fora do cluster por constraint mecânico (hardcoded na always-include de ADR-048) + categoria semântica distinta (meta-doutrina apex vs convenções editoriais estruturais); 2 findings absorvidos caminho-único pelo design-reviewer no /new-adr (F1 trade-off migração retroativa CHANGELOG preservado; F2 invariante de cross-ref bilateral ADR-001↔ADR-051) + 1 F3 informativo registrado (fragment-link user-facing README como sinal a observar pós-merge); F2 Bloco 1 link-rot interno em ADR-024 archived absorvido per precedente Ondas C-G ("body verbatim" aceito); F1 Bloco 3 expandido para 4 gaps consistência editorial (CLAUDE.md:83 + CLAUDE.md:112 + skills/new-adr:55 + skills/triage:169). **3ª instância de refinamento editorial** (Onda F exclusão + Onda G inclusão + Onda H preservação por constraint) — **gatilho de revisão de ADR-045 disparado** + **codificado formalmente via ADR-052** (commit `7d26dc8` + PR #96 merge `fdba1aa`); operador escolheu Opção B (codificação preventiva via ADR sucessor) sobre Opção A (status quo) e Opção C (aguardar 4ª instância). ADR-052 promove 3 modos editoriais para meta-pattern canonical com critério mecânico verificável: (a) EXCLUSÃO + (b) INCLUSÃO + (c) PRESERVAÇÃO POR CONSTRAINT MECÂNICO PURO (modo (c) restrito a hardcode em § Decisão de ADR Aceito vigente; preservação por categoria distinta sem constraint cai em modo (a) bullet 2 per F2 reviewer absorvido opção A); 3 findings absorvidos caminho-único no /new-adr (F1 justificativa cond 4 reforçada; F2 colapso de sub-caso 2; F3 linguagem "promove" vs "cabem dentro"); 1 bloco doc-reviewer 0 findings. Pendência operacional Onda F mantida endereçada: 7 instâncias consecutivas com reviewer-per-bloco. Saldo inventário pós-ADR-052: **31 ADRs vigentes** (30 pós-Onda H + 1 ADR-052; primeiro acréscimo líquido desde Onda A — natural para meta-decisão editorial apex). **Onda Promoção** — tactical-only batch promotion de 7 consolidados Proposto → Aceito formal (ADR-046+047+048+049+050+051+052) gating por F10 cutucada de /triage Onda I — commit `0548130` (plano) + `8ab6a8f` (Bloco 1) + `0c06bde` (captura §3.5) + PR #97 (`c614d6c`); 6 findings absorvidos caminho-único + 1 captura §3.5 (critério lexical bug `^\*\*Status:\*\*` prefix); 4 critérios cumulativos aplicados uniformemente (shipped + effective + referenciado + sem Substituído); auto-bootstrap mecânico documentado (ADR-052 definidor + ADR-048 alvo promovidos juntos sem circularidade lógica). Saldo pós-Promoção: 31 vigentes preservado (zero archived, zero novos). **Onda I** — sétima migração cluster (alinhamento/triage) — draft em branch `onda-i-draft-pending-promotion` pré-Onda Promoção (commits `9540a8f`+`3292242`) → PR #98 (`104a24a`) plan+ADR-053 → /run-plan worktree executou commits `1c9cd9a` (archive 4 ADRs + index 25 linhas) + `4b3ce06` (10 substituições hot spot mecanismo skills/agents incluindo **F4 anexo ADR-035→ADR-043 em code-reviewer.md:64** com anotação histórica) + `344c5ab` (5 substituições foundation+templates mistura categorial intencional per F6) → PR #99 (`8b86960`); ADR-053 absorve ADR-011+026+027+038 (4 dimensões a-d: /draft-idea upstream + wiring + critério + mirror); **primeira aplicação formal de ADR-052 com 2 modos simultâneos** (modo c sobre ADR-009 hardcoded em ADR-048; modo a bullet 1 sobre ADR-042; modo a bullet 2 sobre ADR-031); 6 findings absorvidos caminho-único no /new-adr + 5 no plano /triage + 4 cutucadas F4/F6/F9/F10 decididas pelo operador; 3/3 blocos doc-reviewer 0 findings cumulativos no /run-plan. Saldo inventário pós-Onda I: **28 ADRs vigentes** (53 criados - 25 archived; drop líquido de 3). **Onda J** — oitava migração cluster (bridge cross-project) — commit `5aa5abd` (plano + ADR-054) + PR #100 (`1aa7933`) [4 commits worktree: `12b350f` archive + `cca8192` skills hot spot + `ab90770` foundation + user-facing + `e09b833` captura §3.5]; ADR-054 absorve ADR-032+ADR-042 (2 dimensões a+b: skill /note foundational + store doutrinário non-role + flag --to cross-project write + critério contrato-declarado-vs-heurística); **primeira onda pós-codificação de ADR-052 sem aplicação formal de modos a/b/c** (sketch literal aplicado; pendência editorial de ADR-042 preservado desde Onda I encerrada); 2 findings absorvidos no /new-adr (F1 restauração distinção gates; F2 expansão nota histórica) + 5 no /triage (F1+F2+F4+F6+F7 caminhos-únicos editoriais) + 1 cutucada F3 absorvida opção (a) reduzir escopo da projeção; 4/4 blocos doc-reviewer 0 findings; 1 captura §3.5 (critério 6.4 typo lexical "contrato-declarado" vs "contrato declarado" canonical). Saldo inventário pós-Onda J: **27 ADRs vigentes** (drop líquido de 1; calibração mínima paralela a Onda E) — *refutado pela auditoria da Onda K abaixo: 27 entradas no FS = 26 vigentes substantivos + ADR-035 Substituído preservado in-place per ADR-045 § Decisão parte 1; saldos das Ondas A-J declarados anteriormente sem distinguir Substituído mantidos como snapshot histórico do momento*. **Onda K** — auditoria do inventário vigente (tactical-only paralelo a Onda Promoção) — commits `899a130` (plano /triage) + `d5e6102` (cleanup pré-loop órfãos FS) + `7bf3dc0` (Bloco 2 charter) + `a11bb39` (Bloco 3 BACKLOG) + `e87ddcb` (§3.5 captura) + PR #101 (`17b109a`) — produto: **27 entradas = 26 vigentes substantivos + 1 Substituído preservado in-place (ADR-035)** refuta saldo declarado anterior 27 vigentes; cluster sequence recalibrada (K → K' editorial canonical templates → Promoção II tactical-only 10 candidatos → L discoverability/branding → opcional M foundational templates → apex meta-cluster SKIP recomendado per auto-referência); **target empírico revisado 23-25 ADRs final** em ~3-4 ondas adicionais (vs sketch original 13-15). Pré-loop bloqueio: 6 ADRs órfãos do FS (drive-sync ressuscitando) cleanup via `systemctl --user stop drive-sync.service` + `rm` (commit `d5e6102` separado); 7 descobertas materializadas; 7 findings doc-reviewer absorvidos caminho-único + 1 cutucada ADR-020 absorvida opção (c) preservar standalone; 3/3 blocos doc-reviewer obrigatório (invariante mantido). NÃO migrou cluster (tactical-only paralelo a Onda Promoção); saldo inventário 27 entradas preservado. Ondas K'-X pendentes (cluster sequence recalibrada per Onda K): **Onda K'** — editorial canonical templates — commits `90b3f7f` (plano /triage) + `9e4eab6` (Bloco 1 templates/plan.md) + `edd9556` (Bloco 2 charter linha 189) + `33c315a` (Bloco 3 BACKLOG + sync cross-doc charter) + PR #102 (`c713238`) — produto: refinamento de `templates/plan.md` § Verificação end-to-end com comentário HTML inline contendo 4 diretrizes canonical (prefixar `^\*\*Status:\*\*` em greps de Status field; preferir `git status --porcelain -- <paths>` git-based vs counts lexicais; fidelidade ao texto-alvo via Read antes de hardcodar grep; counts como variável ou condição inversa com aplicação forward apenas). **5 evidências empíricas** absorvidas (Onda Promoção crit 6 `Substituído` sem prefixo Status field + Onda J crit 6.4 `contrato-declarado` com hífen + Onda K crit 1 pré-absorção `wc -l` esperando 33+27 + Onda K crit 6 pré-absorção count==27 esperado + Onda K crit 1 pós-absorção raw 27+filtrado 26 vs esperado 33+27); cleanup órfãos FS pré-loop não codificado como evidência do pattern lexical (vive como item BACKLOG categoria distinta drive-sync ressuscitação pós-merge registrado durante Onda K commit `d5e6102`). Tactical-only paralelo a Onda Promoção; sem ADR criado per intent + decisão /triage opção (a) + auto-aplicação cond 4 ADR-034 leitura estreita ADR-052 (sub-estrutura do eixo templates canonical sem 4º eixo conceitual). 6 findings doc-reviewer absorvidos caminho-único pré-commit no /triage + 1 cutucada count 4 vs 5 absorvida opção (a) padronizar 5 + 1 [FORTE] Bloco 1 + 1 [MÉDIO] Bloco 2 absorvidos caminho-único; 3/3 blocos doc-reviewer obrigatório (invariante mantido). Saldo inventário 27 entradas preservado. → **Onda Promoção II** — tactical-only batch promotion 10 ADRs Proposto-shipped → Aceito formal (2026-06-01) — commits `4e443ba` (plano /triage) + `52e0a0e` (Bloco 1 Status promotion 10 ADRs) + `0fef636` (Bloco 2 charter dual edit linha 188+190) + `bcf8f33` (Bloco 3 BACKLOG) + PR #103 (`1adff56`) — produto: Status field promotion em 10 ADRs (ADR-022, 031, 033, 034, 036, 037, 043, 045, 053, 054) com critério explícito clarificado **Proposto-shipped + efetivo (referenciado como autoridade em CLAUDE.md/skills/agents) + Status atual definitivo no momento (cluster downstream futuro não invalida)**. 3 categorias expandidas vs Promoção I: (1) apex doutrinais não-promovidos na Promoção I (ADR-043 + ADR-045); (2) consolidados Onda I+J (ADR-053 + ADR-054); (3) standalone preservados (sub-3a ADR-031+ADR-034) + candidatos cluster downstream sequenciado (sub-3b ADR-033 Onda M + ADR-037 Onda L) + **standalone materializados nesta onda por unicidade categórica (sub-3c ADR-022 + ADR-036)** — decisão substantiva pequena via cutucada (b) absorvida do design-reviewer. Auto-bootstrap mecânico: ADR-052 + ADR-048 preservam IDs hardcoded. **Saldo quantitativo inalterado (27 entradas; zero archived; zero novos); saldo qualitativo alinha distribuição lifecycle** — 10 Proposto → Aceito; charter linha 188 atualizada movendo ADR-022/036 de candidatos cluster (10→8) para standalone preservados (3→5). 4 findings doc-reviewer absorvidos caminho-único pré-commit no /triage + 1 cutucada (b) materializar decisão substantiva + 1 [MÉDIO] Bloco 2 absorvido caminho-único; 3/3 blocos doc-reviewer obrigatório (invariante mantido). → **Onda L** — preservação ADR-037 standalone via sub-3c 2ª instância (tactical-only paralela a Promoção II) — commits `cef5b19` (plano /triage) + `7398a0a` (Bloco 1 charter) + `87a8830` (Bloco 2 BACKLOG) + PR #104 (`c92a211`) — produto: aplicação editorial de sub-3c "standalone por unicidade categórica" ao ADR-037 (Código-como-fonte-de-verdade vs intent-as-truth; categoria apex única primazia code/doctrine sem família plausível). 2ª aplicação empírica do pattern emergente em Promoção II (1ª: ADR-022 + ADR-036). Cluster discoverability/branding do sketch original dissolvido editorialmente (único membro vivo preservado; ADR-012 — outro membro do cluster — absorvido em Onda H via ADR-051; cluster bridge cross-project vizinho ADR-032+042 consolidado em Onda J via ADR-054 distintamente). Caminho B escolhido sobre A (modo (a) bullet 2 leitura ampla — não casa sob leitura literal exigindo "ancestral codificado direto") e C (consolidação ADR-055 temática — pseudo-consolidação 1→1 sem ganho semântico, contradiz Ockham operacionalizado per ADR-043). **Contador sub-3c: 2/3** para ADR-052 § Gatilhos linha 187 ("4º modo editorial emerge em Ondas I-X"); 3ª aplicação futura dispara refinamento ADR-052 codificando sub-3c como modo (d) formal. Saldo inventário 27 entradas preservado (zero archived, zero novos; ADR-037 já Aceito via Promoção II). Tactical-only doc-only; sem ADR criado. 3 findings design-reviewer absorvidos caminho-único pré-commit no /triage (F1 grep discriminante; F2 precisão cross-doc; F3 awk → grep portável) + 2 findings doc-reviewer absorvidos caminho-único no Bloco 1 (heading § Refinamento editorial estendido; label "Código-como-fonte-de-verdade" canonical); 0 cutucadas no enum (operador clarificou antes do disparo e confirmou recomendação caminho B em prosa); 2/2 blocos doc-reviewer obrigatório (invariante 17+ instâncias preservado). → **Onda M** — primeira consolidação cluster pós-Promoção II (cluster foundational templates) — commits `f44c62c` (plano /triage + ADR-055) + PR #105 (`8ae8e97`) [2 commits worktree: `a7278ca` Bloco 1 archive + `bbb2530` Bloco 2 cross-refs] — produto: ADR-055 absorve ADR-001 (foundational — protocolo templates centralizados) + ADR-033 (sucessor parcial primário estendendo critério single-consumer-declarativo) per ADR-034 cond 5 primária isolada. Família semântica clara codificada via sucessor parcial (ADR-033 § Origem citava ADR-001 § Decisão linha 33; ADR-001 Addendum 2026-05-16 citava ADR-033). Sub-3c NÃO dispara (família plausível ENTRE ADR-001 e ADR-033; contador permanece 2/3 paralelo a Onda L; gatilho refinamento ADR-052 § Gatilhos linha 187 aguarda 3ª aplicação genuína futura). Anti-regression: cross-ref bilateral ADR-055↔ADR-051 substitui ADR-001↔ADR-051 (charter linhas 245-247). ADR-035 linha 84 + ADR-034 linhas 38/44/60 NÃO tocadas (preservação editorial intencional — exemplos pedagógicos históricos in-place). **Pattern editorial novo emergente registrado em 2 instâncias na Onda M:** (1) preservação seletiva exemplos pedagógicos vs cobertura completa em ADRs vigentes citando ADRs absorvidos (ADR-034 38/44/60 — cutucada F1 /triage opção (b)); (2) variação editorial blockquote redirect (§ Decisão (a)/(b) específica vs forma canonical genérica — F1 /run-plan Bloco 1 caminho-único). 2ª/3ª aplicação futura pode materializar como pattern canonical refinado. **Saldo inventário pós-Onda M: 26 entradas no FS** (drop líquido de 1 vs pré-Onda completa; primeira consolidação cluster pós-Promoção II — avanço concreto para target empírico 23-25 ADRs final). 3 findings design-reviewer absorvidos caminho-único no /new-adr (F1 contagem 11ª; F4 clareza alternativa (d); F7 referência semântica templates/plan.md) + 5 caminho-único no /triage (F2 cross-ref Onda K' #3; F3 invariante semântica; F4 contador sub-3c precedentes; F5 aritmética pré-archive 28; F6 safety net grep) + 1 cutucada opção (b) /triage (F1 ADR-034 38/44/60) + 1 caminho-único /run-plan Bloco 1 (F1 variação blockquote § específica); 2/2 blocos doc-reviewer obrigatório (invariante 21+ instâncias consecutivas preservado). → **Onda Final A** — admission policy enforcement runtime (parte 1 da Onda final do charter linha 35) — commits `e8e507d` (plano /triage) + PR #106 (`b172689`) [2 commits worktree: `e7f3ae4` Bloco 1 step 3.5 em /new-adr + `957282a` Bloco 2 design-reviewer § O que flagrar critério novo] — produto: 2 superfícies runtime efetivas materializam ADR-045 § Decisão parte 2 (filtro de admissão going forward): (1) /new-adr step 3.5 prompt informativo AskUserQuestion filtro 3 saídas (ADR Recommended / CLAUDE.md ou philosophy.md / git log) com heurísticas + critérios de desempate; (2) design-reviewer ganha critério "Candidato a ADR draft que falha no filtro de admissão" em § O que flagrar para audit drift pós-criação. Belt-and-suspenders. **ADR-045 § Condição de validade satisfeita** (≥1 superfície runtime efetiva; na verdade 2); **Onda Final B (release v3.0.0) liberada** pós-merge. Caminho A (2 superfícies primárias canonical) escolhido sobre caminho B (apenas design-reviewer fallback). **1ª aplicação convenção decimal-retroativa intercalada** (step 3.5 entre step 3 e step 4) registrada honestamente; ADR-045 § Decisão parte 2 linha 88 "provavelmente 3.5" honrado literal. Saldo inventário 26 entradas preservado (sem ADR criado). 6 findings design-reviewer caminho-único + 1 cutucada (b) /triage + 2 caminho-único /run-plan Bloco 1; 2/2 blocos doc-reviewer obrigatório (invariante 23+ instâncias consecutivas preservado). → **apex meta-cluster SKIP recomendado** per NOTES *"risco alto auto-referência"* + ADR-045 sendo absorvido por consolidado que ele autorizou.

- plugin: avaliar step preventivo de detecção de ADRs órfãos em `docs/decisions/` em `/run-plan` pré-loop ou `/triage` step 0 cleanup pós-merge — durante `/run-plan` Onda K (2026-06-01) detectados 6 ADRs órfãos do FS (versões pré-archive das Ondas I+J: 011/026/027/032/038/042) bloqueando setup. Causa-raiz suspeita: **drive-sync (rclone bisync) ressuscitando arquivos pós-merge** (ondas C-H sem registro; investigação primária vive no meta-system/drive-sync config — filters/excludes para archive paths git-managed). Mitigação imediata: `systemctl --user stop drive-sync.service` + `rm` dos órfãos antes de prosseguir. Toolkit pode prevenir bloqueio em runs futuros independente da causa-raiz via detecção `git status --porcelain | grep -E '^\?\? <decisions_dir>/ADR-'` no `/run-plan` setup ou `/triage` step 0. Reavaliar se segundo incidente em outra onda emergir; sem ele, editorial pendente.

- plugin: avaliar ADR sobre **partição editorial dual-audience** — distinção entre doutrina pública do plugin (`philosophy.md`, `README`, `agents/skills`) e doutrina pessoal do autor em repo separado (`meta-system` `ARCHITECTURE.md`). Reviewer (design-reviewer) flagrou no plano `principios-fundamentais-philosophy` como sucessor parcial potencial de ADR-051 § Decisão (a)+(b) (idiomas por audiência consolidado pós-Onda H absorvendo ADR-007+012) com **novo eixo cross-repo** (público-do-plugin vs pessoal-do-autor). Per ADR-045 § Decisão parte 2 admission policy: filtro de 3 saídas se aplica — verificar reversibilidade positiva (cenário concreto de reversão), categoria nova (eixo cross-repo), restrição externa, ou pattern emergente (≥3 aplicações ad hoc). **Atualização 2026-06-01:** redesign da camada doutrinal completa (Ondas A-M + Onda Final A admission policy enforcement; PR #106); este item agora é candidato natural para `/triage` independente pós-v3.0.0 como **primeira aplicação concreta do admission policy enforced em runtime** — step 3.5 de /new-adr filtrará via heurísticas; design-reviewer auditará drift. Decisão substantiva diferida para pós-bump v3.0.0.

- plugin: avaliar ADR sobre "fronteira de blast-radius compartilhado" — code-reviewer no Bloco 1 do plano `run-plan-falha-worktree-branch-checked-out` sugeriu codificar como decisão estrutural a regra de que skills do plugin não emitem comandos que mutem estado fora da worktree corrente (working tree principal, refs locais, branches de terceiros). Hoje a regra vive em CLAUDE.md global ("ações de blast-radius compartilhado exigem confirmação") e foi aplicada localmente em `/run-plan §1.1` via bullet do `## O que NÃO fazer`, mas não está codificada como ADR. Custo: próximo incidente análogo em outra skill reabre a doutrina do zero. Direção: ADR enumerando a fronteira (worktree corrente apenas) + tabela retroativa de aplicação. Reavaliar se segundo incidente em outra skill aparecer; sem ele, fica como editorial local.
- plugin: paralelizar verificação "já implementado?" em `/next` via Agent (Explore) por candidato — spawn 1 subagent por linha de `## Próximos`, retorna verdict (sim/não/parcial + path). Hoje serial em main; latência cresce linearmente com tamanho do backlog. YAGNI até backlog crescer ≥20 itens ou pain real surgir; backlog atual (~3 itens em Próximos) não justifica overhead de subagent.
- plugin: avaliar nota "Convenção de evidência antes de claim" em `docs/philosophy.md` — análise comparativa com obra/superpowers (skill `verification-before-completion`, "evidence before claims, always"). Toolkit já implementa implicitamente via gate de validação manual em `/run-plan` e passo 2 (Reproduzir) de `/debug`, sem nomear. Direção: 1 parágrafo formalizando a regra (sem skill nova — choca com YAGNI), reduzindo drift quando reviewers/skills afirmam sucesso sem comando rodado. Reavaliar se drift concreto observado em PR ou diagnóstico — se reviewers já operam assim na prática, é redundante.
- plugin: avaliar sub-bloco "tracing reverso" em `/debug` passo 3 (Isolar) — análise comparativa com obra/superpowers (`root-cause-tracing.md` mostra exemplo concreto de 5 níveis de stack até origem `tempDir: ''`). Toolkit menciona "stack trace → módulo/função, git blame" sem template mental concreto de tracing reverso. Direção: 5-10 linhas com mini-exemplo. Trade-off: aumenta SKILL.md (ADR-008/philosophy.md favorecem flatness). Reavaliar se uso real de `/debug` mostrar diagnósticos parando em causa imediata em vez de causa-raiz; sem sinal de subutilização, skip.
- plugin: atualizar exemplo "Recommended dinâmico" em CLAUDE.md → "AskUserQuestion mechanics" — cita `/run-plan §3.7 escolhe Recommended por plans_dir mode` como caso de construção dinâmica; após [ADR-028](docs/decisions/ADR-028-campo-branch-opcional-plano-fluxo-issue-first.md) o critério ficou bidimensional (modo `plans_dir` × presença do campo `**Branch:**` no plano). Sinalizado pelo `code-reviewer` como observação fora-de-escopo no Bloco 3 do plano `campo-branch-opcional-plano-issue-first`. Onda editorial futura.
- plugin: cenário 1 de `## Verificação manual` do plano `run-plan-3-3-skip-empirico` é editorialmente inconsistente com a regra implementada — descreve grep com filtros restritivos (`step 0`, `cleanup pós-merge`), não os identificadores literais que a regra usa (filename base `triage` + `/triage` do Resumo); pela regra literal a fixture produziria matches espúrios em `README.md:9`, `docs/install.md:36/42/45/65` e cairia em cutucada-com-referrer-espúrio absorvida por `Consistente`, não skip silente. Reformular: (a) usar fixture com identificador genuinamente ausente em docs, ou (b) reescrever cenário 1 descrevendo o caminho real (cutucada com referrer espúrio → descartar via Consistente). Flagado durante `/run-plan run-plan-3-3-skip-empirico` (passo 3.2); fora-de-escopo da onda editorial 2026-05-15 (refactor editorial do próprio plano, não validação de comportamento). Reavaliar em onda editorial dedicada ou em invocação de `/triage` específica.

- plugin: corrigir drift em `docs/procedures/reviewer-invocation-read.md` citando `/run-plan §2.3` — identificador inexistente (`skills/run-plan/SKILL.md` tem só § 1/2/3 sem sub-numeração). README corrigido análogo para `§2 loop por bloco` via PR do plano `readme-completar-tabela-templates-procedures` (commit `df3fffe`); procedure file precisa fix análogo (1 linha edit, doc-only, baixo risco). Flagrado pelo doc-reviewer Bloco 1 desse mesmo /run-plan; capturado via §3.5 como fora-do-escopo do plano corrente (escopo era só `README.md`).

## Concluídos

- plugin: avaliar generalização da saída `CLAUDE.md ou philosophy.md` do filtro de admissão ([ADR-045](docs/decisions/ADR-045-redesign-camada-doutrinal-consolidacao-politica-admissao.md) § Decisão parte 2; superfícies runtime em `/new-adr` step 3.5 + `design-reviewer` § O que flagrar "Candidato a ADR draft que falha no filtro de admissão") — texto da saída assume vocabulário interno do plugin (`philosophy.md` existe só neste repo); projetos consumidores tipicamente têm apenas `CLAUDE.md`, criando zona cinzenta sobre destino do conteúdo redirecionado. Heurísticas universais (reversibilidade nomeável, categoria nova, restrição externa, pattern ≥3 aplicações) e saída `git log` funcionam igual em qualquer projeto — fricção concentra-se na bifurcação `CLAUDE.md` vs `philosophy.md`. Direções possíveis: (a) generalizar linguagem da saída para "documento doutrinal canonical do projeto" (resolver no contexto local); (b) resolver `philosophy.md` via role opt-in no `<!-- pragmatic-toolkit:config -->`. Sem instância empírica de fricção hoje; reavaliar quando operador em consumidor relatar confusão recorrente, ou quando 2ª aplicação concreta do admission policy em projeto consumidor surfar a fricção. Origem: conversa CC 2026-06-02 elicitando aplicabilidade do mecanismo de admissão fora do próprio plugin. **shipped 2026-06-02:** caminho (a) generalização editorial implementado via `/run-plan filtro-admissao-vocabulario-consumidor` — commits `c2abf27` (skills/new-adr/SKILL.md + agents/design-reviewer.md) + `8a68527` (README.md align) + PR #107 (merge `5a441b9`). Bifurcação mecânica vs epistêmica preservada via "ou equivalente do projeto" / "ex.: deste plugin". Direção (b) role `philosophy_doc` opt-in NÃO implementada — sem precedente empírico justificando categoria nova de role; preservada como direção alternativa se sinal empírico futuro surfar (operador em consumidor relata fricção, ou 2ª aplicação concreta do admission policy em consumer).

- plugin: `/run-plan §Publicar` PR # follow-up YAGNI confirmado pós-disparo do gatilho — **fechado 2026-06-01:** gatilho declarado "≥4 ondas" disparou (16+ ondas pós-redesign); verificação empírica confirma convenção informal funcionando sem o gate proposto (charter § Atualização pós-execução + chores post-merge têm PR # consistente em todas as ondas A-Final A). YAGNI venceu. Item original (registrado pré-redesign): "endurecer follow-up 'atualizar PR # em roadmap entries shippados via Bloco 5 do plano corrente' — hoje depende de memória do operador; doc-reviewer flagrou ausência como follow-up acionável em `procedures-cleanup-pos-merge`; direção: gate `Publicar` detectar `[x]` em `docs/audits/runs/*-execution-roadmap.md` sem PR # e cutucar operador. YAGNI até passar das 4 ondas; registrar para reavaliar". Reavaliação pós-disparo confirma YAGNI — sem gate operacional necessário.
- plugin: **reforma doutrinária — hierarquia invertida (✓ COMPLETA — 4 ondas shippadas 2026-05-29):** 3 princípios fundamentais (Verdade, Excelência sem over-engineering, Navalha de Ockham) shippados em PR #84 codificados como raiz epistêmica do toolkit via ADR-043; YAGNI/flat/sem-defensividade-ornamental tornaram-se consequência operacional derivada. **Onda 1 — ADR de inversão** (commit `076354f`): ADR-043 substitui ADR-035 absorvendo seus 4 critérios como § "Ockham operacionalizado em decisões internas do plugin"; declara efeito retroativo explícito (mitiga meta-loop do design-reviewer). **Onda 2 — reframing batch** (PR #85, commits `b7c85a2`/`34475d1`/`4da8bf1`/`a650ace`): adendos cross-ref aos fundamentais em ADR-014/-016/-032/-036 (decisões centrais intactas; vocabulário YAGNI-as-veto modernizado para Ockham/Verdade/Excelência operacionalizados). **Onda 3 — consolidação editorial** (PR #86, commits `5b083b1`/`11f4076`): cluster index addenda em ADR-005 (modo local; sucessores ADR-018/-025/-030) e ADR-017 (cutucadas; sucessor ADR-029 + `docs/procedures/cutucada-descoberta.md`). **Onda 4 — free-read refinement** (PR #87, commits `9b8790e`/`e9f4785`): ADR-044 (sucessor parcial de ADR-021) codifica scan medium + always-include curated list (ADR-009/-034/-043 sempre lidos integralmente paralelo a `philosophy.md`); `agents/design-reviewer.md` § Curadoria atualizada; CLAUDE.md ganha bullet de cross-ref; ADR-021 ganha Addendum 2026-05-29 (paralelo a ADR-005/-017 Addenda da Onda 3). **Alavanca de token cost real** materializada: baseline pré-refinamento empírico ~36k tokens free-read (recursive moment do reviewer Bloco 1 Onda 4); pós-merge esperado ~15k (~58% redução). Validação empírica em invocações subsequentes do design-reviewer fica como spec pós-fato. Reforma codificou hierarquia doutrinal apex + reframou 4 ADRs históricos + consolidou 2 clusters densos + reduziu token cost — todas as 4 ondas usaram dogfood end-to-end (`/triage` → `/new-adr` ou `/run-plan` → design-reviewer auto-fire → cutucadas absorvidas/cutucadas resolvidas → commit unificado).
- plugin: imprimir 3 princípios fundamentais (Verdade, Excelência, Ockham) em `docs/philosophy.md` como raízes epistêmicas da filosofia pragmática já declarada (YAGNI/flat/sem-abstrações/sem-defensividade ornamental) — proposta surgida em sessão CC no consumer `meta-system` em 2026-05-28. Substância dos 3: **Busca pela verdade** (o que existe, existe — verificar antes de assumir, medir antes de generalizar, falsificável > hand-waving); **Excelência sem over-engineering** (qualidade serve o problema concreto, não vice-versa; YAGNI bounds escopo, excelência drives qualidade dentro do escopo); **Navalha de Ockham** (não multiplicar entidades — operacionaliza YAGNI no dimensional: não só "não implementar o que não usa" mas "não modelar o que não distingue"). Q1 do operador (no topo, antes de "A filosofia em uma frase") e Q3 (Verdade → Excelência → Ockham) prevaleceram sobre recomendações inversas do design-reviewer; Q2 adiou ADR sobre partição editorial dual-audience como nova linha em Próximos per ADR-035 critério 4. 4 design-reviewer findings absorvidos no /triage (filtro ADR-035 explícito, gabarito anti-prescritivo mecanizado, fechamento de defaults (c)/(d), greps de verificação reforçados); zero findings do doc-reviewer no Bloco 1.
- plugin: skill `/note` aceita flag `--to <projeto-ou-path>` para write cross-project (per ADR-042; discovery via `$PROJECTS_DIR` com fallback a path absoluto; pré-condição de target inicializado preserva blast-radius; gate `Worktree replication` não roda em cross-write).
- plugin: gate Worktree replication no /note via probe-and-add idempotente — fecha gap ADR-018/ADR-032 (NOTES.md como store non-role, independente de role local declarada). ADR-018 ganha § Addendum (2026-05-27) reconhecendo /note como 2º dispatcher universal (vs init-config condicional); justificativa adendo vs novo ADR amarrada ao critério ADR-034 (assimetria de eixo vs ADR-030: novo dispatcher para mesmo path ≠ novo path para mesmo dispatcher). ADR-005:48/75 e ADR-032 § Decisão recebem cross-refs. /note SKILL.md passo 1 materializa probe regex `^\.claude(/|$)` + 3 ramos + ordem determinística dos gates (Gitignore primeiro, Worktree replication em seguida; cancel aborta) com cross-ref textual ao step 4.5 do /init-config como fonte canonical do pattern (drift control); 4º dispatcher emergir → extrair para docs/procedures/. docs/install.md atualizado para citar ambos dispatchers. 1 finding doc-reviewer absorvido pré-commit (precisão "linha .claude/ da tabela composta"); 4 findings design-reviewer absorvidos no /triage (ADR-005 cross-ref, gatilho fechamento, ordem dos gates, ADR-032 § Decisão). Validação manual (7 cenários) fica como spec pós-release.
- plugin: campo `**Modo:** runbook` opt-in em planos para `/run-plan` cobrir system-surgery — primeiro caso empírico (plano `onda-1-fs-migration` do consumer `meta-system`: worktree dentro do FS migrado, 11/12 blocos sem diff git, rollback fora do git, reviewer sobre diff inexistente, validação intercalada). ADR-041 sucessor parcial lateral de ADR-002 + pré-condição 0 + `## Modo runbook` section em `/run-plan` SKILL.md (tabela 4 dimensões: sem worktree / sem commit-per-bloco / gate por bloco / validação intercalada) + materialização de capturas no `Falhou` (paralelo §3.5) + entrada no templates/plan.md + bullet em CLAUDE.md. Operador escolheu (b) modo opt-in sobre (a) recusar / (c) só ADR via `/triage`; codificação com 1 evidência absorvida sob ADR-035 critério 2 (fronteira doutrinal nítida). 4 cutucadas do design-reviewer resolvidas (F2 materializar capturas no `Falhou`, F3 único valor `runbook`, F4 bloqueio duro `**Branch:** + **Modo:** runbook`, F8 cenários 6+7 adicionados); 5 findings absorvidos pré-commit. Smoke real em consumer `meta-system` Onda 2/3/4 fica como spec pós-release (Cenário 5 da `## Verificação manual`).
- plugin: hook block_settings_drift para auto-bloquear paths absolutos e session perms em .claude/settings.json — ROADMAP item 5 (último da Onda 1; fecha o plugin todo: Onda 1 5/5 + Onda 2 4/4). Terceiro PreToolUse block hook do plugin (paralelo a block_env/ADR-015 e block_gitignored/ADR-016), sucessor parcial lateral de ADR-015 per ADR-034 Cond 5. Bloqueia edits a `.claude/settings.json` introduzindo `/home/<user>/` ou `/Users/<user>/` via content regex bruta (operador escolheu sobre JSON parse — F3+F4 cutucada). Escopo restrito a paths absolutos (operador escolheu sobre cobertura ampla — F1 cutucada); session perms → Limitação + Gatilho de revisão. Novo ADR-040 reconhece tensões com ADR-018/ADR-005 (`.claude/` território Claude Code → defesa: gate defensivo ≠ modificação proativa), ADR-016 (consumer signal first → defesa: tracked sem `.gitignore` a respeitar, pattern universal), ADR-015 § Alt (d) (content inspection no hot path → defesa: escopo restrito + formato estável). 3 blocos (ADR + script + hooks.json) + 1 extra (README/install.md). Friction recorrente confirmada por /insights report (cleanup cycles em settings.json).
- plugin: /new-adr clarifica spec "Não inventar" vs "preencher com inputs do operador" — ROADMAP item 9 (último da Onda 2). Refinamento editorial puro do bullet em `skills/new-adr/SKILL.md ## O que NÃO fazer` distinguindo "inventar" (sem base, proibido) de "preencher" (com inputs explícitos do operador em ROADMAP / plano upstream / conversa / entrevista, esperado). Passo 4 ("Criar arquivo") alinhado pelo mesmo princípio (era "Não preencher conteúdo", agora "preencher de inputs; placeholders só como fallback"). Cross-ref a ADR-011 (design-reviewer audita drift input→síntese). Codifica comportamento empírico observado em ADR-037/038/039 desta sessão. Sem novo mecanismo, sem ADR — refinamento intra-skill (regra vive onde é consumida); rebut articulado de adendo cross-ref a ADR-027/026 per ADR-034 categorias.
- plugin: captura automática /run-plan §3.5 via TaskCreate com marker — ROADMAP item 8 (Onda 2); resolve fragilidade latente "lista mental do agente" em /run-plan §3.5 captura automática (mesma classe de silent regression que itens 6+7). Cria ADR-039 sucessor parcial de ADR-010 estabelecendo categoria nova "Task tool como state-keeping em fluxo longo" (paralela a "progress display" de ADR-010); marker convention `[capture:validacao]` / `[capture:backlog]` no subject; lifecycle 2-estados `pending → completed` skipping `in_progress` (semântica state-keeping ≠ cursor de execução). /run-plan SKILL.md edita 3 locais (tabela warnings pré-loop, prelúdio Captura automática Deferida, §3.5 materialização) substituindo lista mental por TaskCreate/TaskList unificado nas 3 superfícies emissoras (pré-loop ADR-002 + passo 2 loop + passo 3.2 manual). Pattern paralelo a ADR-038 (mesma onda, mesmo critério ADR-034 5).
- plugin: Read explícito antes de análise nos prompts dos reviewers — ROADMAP item 7 (Onda 2); resolve stale-view observado em item 4 §3.4 da Onda 1 (sessão 2026-05-26). Novo `docs/procedures/reviewer-invocation-read.md` (categoria ADR-024) com a instrução canonical; 3 SKILLs (`/run-plan` §2.3, `/triage` step 5, `/new-adr` step 5) referenciam o procedure na composição do prompt do reviewer. Caller-side per `code-reviewer.md:10` "apenas o diff" doctrine constraint (agent-side conflitaria com diff-only). Manual `@reviewer` não coberto (gap aceito, baixa incidência). Forçar refresh defensivo via `Read` cobre múltiplos cenários de causa raiz (Edit→Agent timing, snapshot estático, cache de tool result) sem depender da raiz exata.
- plugin: embedar Decisões absorvidas no body do plano para code-reviewer consumir — ROADMAP item 6 (primeiro da Onda 2); resolve fragilidade reviewer-vs-reviewer observada no item 1 (sessão 2026-05-26). Mirror de `## design-reviewer findings absorvidos` (commit message ADR-026) em nova seção opcional `## Decisões absorvidas` no body do plano; `/run-plan` §2.3 lê e passa como contexto uniform-protocol a todos os reviewers; `code-reviewer` agent def trata estruturas listadas como out-of-scope da rubrica YAGNI. Cria ADR-038 sucessor parcial de ADR-026 (estende Forma) + refina § Decisão de ADR-035 (categoria nova "context-aware via messenger upstream" distinta da rejeitada "free-read autônomo"). 5 blocos editoriais (template → ADR-038 → reader → consumer → writer último ativa mecanismo). Override-por-inação ADR-035 vira mecânico em vez de remediativo.
- plugin: README com framing Product Engineer harness e contraste spec-first — ROADMAP item 4 (destravado por ADR-037, commit `fe97ab6`); parágrafo de posicionamento adicionado no topo do README entre tagline e `## What's inside` framing o plugin como "Product Engineer harness" (vocabulário do vídeo SV 2026) + contraste curto descritivo com spec-first generativo (ex.: github/spec-kit), link inline a ADR-037 para aprofundar. EN per ADR-012; tom descritivo (não combativo). Absorve marketplace prep #7 no mesmo plano.
- plugin: marketplace prep #7 (polimento) — absorvido pelo ROADMAP item 4 / plano `readme-framing-product-engineer` que entregou o mesmo goal de discoverability EN via prose framing "Product Engineer harness" no topo do README, com contraste curto a spec-first generativo e link inline a ADR-037. Visual escalation original (walkthrough/GIFs/snippet de transcrição) pode reabrir se métrica de engajamento futura mostrar gap — intenção original preservada como gatilho.
- plugin: /triage e /next leem .claude/local/NOTES.md como contexto suplementar — ROADMAP item 2; fecha o loop write→read do `/note`. Mecanismo prose (não frontmatter role) preserva ADR-032 (store non-role). `/triage` step 1 ganha sub-item 6 e `/next` step 1 ganha parágrafo análogo, ambos com cross-ref a ADR-032, condicional "se existir", cláusula informational/não-bloqueante e instrução explícita de reporte (auditabilidade). Escopo limitado a /triage+/next (read-side antes de produzir decisão); /run-plan, /debug, /draft-idea excluídos com justificativa. README.md:10 ganha sentença sobre consumo intra-project. Convergência tripla: vídeo SV 2026 (MCP-to-prod), spec-kit (bidirectional feedback), uso manual estabelecido (memória `feedback_parallel_sessions.md`).
- plugin: cutucada de decomposição multi-plano em /triage step 2 — ROADMAP item 1; novo bullet "Tamanho / decomposição" com heurística qualitativa (operador consegue listar ≥3 facetas semanticamente independentes na descrição) dispara cutucada enum `Decomposição` (`Enumerar facetas como planos separados` / `Manter como plano único`); 3 fronteiras enumeradas (Intenção vaga / Bifurcação arquitetural / Escopo) + nota de precedência (Intenção vaga precede). Bullet `Escopo` intensificado com cross-ref. Convergência de 3 análises: vídeo SV 2026 ("agent-sized chunks"), contraste com spec-kit (`/tasks` dependency-mapped), gap multi-plano confirmado pelo operador (≥3 ocasiões históricas).
- plugin: refactor `docs/procedures/cutucada-descoberta.md` — `## Gating tri-state` (tabela declarativa) virou `## Algoritmo` numerado prescritivo; passo 1 emite `Bash` literal mapeando stdout `NO_FILE` / `NO_MARKER` / `MARKER` para próxima ação; passos 2-3 cobrem dedup conversation-scoped e emissão. Doutrina ADR-017 + ADR-029 intacta — refactor é da mecânica de execução (camada abaixo da decisão); sem ADR/adendo per ADR-034. Bug evidenciado em sessão `remove-n8n` do consumer `h3-finance-agent` (4 emissões de string-A com marker presente em `CLAUDE.md:93`, zero greps no log da sessão) preservado como exemplo dentro do próprio procedure.
- plugin: aplicar [ADR-036](docs/decisions/ADR-036-brainstorm-intencionalmente-nao-codificado-em-skill.md) em `/draft-idea` passo 1.5 + `/triage` step 2 — (a) refinar redirect text da opção `Direção de feature → /triage` no passo 1.5 do `/draft-idea` para reconhecer que feature-com-intenção-vaga tem como porta natural raw-chat antes de `/triage` (evita landing fraco em skill que pressupõe intenção formada); (b) adicionar cutucada de "intenção vaga demais para triar" no checklist mental do `/triage` step 2 com heurística "verbo abstrato sem critério operacional + objeto-direto ausente", em prosa (não enum), sugerindo exploração raw antes ou pedindo objeto/critério mais firme, com fronteira explícita vs bifurcação arquitetural (que assume intenção formada com 2 caminhos). Doc-only, edits cirúrgicos cross-skill; bundle único de commit.
- plugin: extrair auto-detect de forge (parse remote → gh/glab/fallback) para `docs/procedures/forge-auto-detect.md` — consolida mecânica hoje duplicada em 4 SKILLs (`/release §5`, `/next §4.5`, `/run-plan §3.7`, `/archive-plans §1` critério 6) + `docs/procedures/cleanup-pos-merge.md`. Onda 3 parte 2 (B_prose) do roadmap 2026-05-15/16; promove categoria `docs/procedures/` para 3 itens (fortalece ADR-024 para promoção via A_arch).
- plugin: extrair cutucada de descoberta (gating tri-state + 2 strings canonical) para `docs/procedures/cutucada-descoberta.md` — consolida mecânica hoje duplicada em 5 SKILLs (`/triage`, `/run-plan`, `/new-adr`, `/next`, `/draft-idea`); CLAUDE.md `## Cutucada de descoberta` trima para scope + herança editorial + ref ao procedure. Onda 3 parte 1 (G_arch ≡ C_prose) do roadmap 2026-05-15/16; bundle com Onda 3 parte 2 (B_prose) move categoria `docs/procedures/` para 3 itens.
- plugin: encurtar 4 descriptions auto-loaded (`security-reviewer`, `init-config`, `triage`, `release`) — remover frases finais que duplicam categorias da primeira ou repetem o gatilho do `name`; ~50 w líquidos a cada turn da sessão (~65 tokens). A_prose da Onda 2 do roadmap 2026-05-15/16, encaminhado como commit cirúrgico direto sem `/triage` per encaminhamento explícito.
- plugin: criar `docs/audits/execution-roadmap.md` como terceiro prompt de auditoria — consome 1-N runs em `docs/audits/runs/` e produz roadmap consolidado identificando sobreposições entre eixos (ex.: G_arch ≡ C_prose no ciclo 2026-05-15/16) e dependências cruzadas. Preserva os 2 prompts atuais (`architecture-logic.md`, `prose-tokens.md`) com critérios distintos por eixo — cada run continua sendo entregável por si. Padrão paralelo a `docs/procedures/` (artefato compartilhado). ADRs vizinhos: ADR-001 (templates centralizados, critério de compartilhamento), ADR-024 (categoria `docs/procedures/`, hoje Medium por "categoria com 1 item suspeita"). Motivação: ciclo 2026-05-15/16 mostrou que síntese manual do roadmap repete trabalho de identificar sobreposições e ordenar dependências; prompt dedicado captura o algoritmo como invariante. Reavaliar como plano quando entrar em fila — sub-variantes de mecânica de consumo dos runs (standalone vs invocado-ao-final-da-2ª-auditoria vs argumento-lista-runs) têm trade-offs reais.
- plugin: skill `/note <conteúdo>` para captura de contexto compartilhado em `.claude/local/NOTES.md` (modo local-gitignored estendendo ADR-005 para store non-role); cobre paralelismo intra-projeto + cross-project via referência conversacional do operador.
- plugin: onda editorial 2026-05-15 — fechar pendências de validação ativas em 4 planos + promover 7 ADRs Strong de Proposto→Aceito.
- plugin: executar propostas das auditorias 2026-05-12 — roadmap em `docs/audits/runs/2026-05-12-execution-roadmap.md` declara "Roadmap 2026-05-12 totalmente cumprido (2026-05-12). 4 ondas + item Diferido fechados": Onda 1 (3 ADRs C/B/E_arch via PRs #54-#55), Onda 2 (A_arch/A_prose/C_prose via PR #56), Onda 3a (G/D_arch via PR #57), Onda 3b (B/D_prose via PR #58), Onda 4 (E/F/G_prose + A/B/C/D-NEW via PR #59; F_arch via commit `066e1ae`); H_arch reaberto pós-diferimento e shipped via PR #60 (ADR-025 — recusar cross-mode backlog local + plans canonical). Total: 6 ADRs novos (020-025), 5 planos shippados, 5 PRs + 1 commit cirúrgico direto.
- plugin: `/run-plan §1.1` cobre falha de `git worktree add` por branch já checked out em outro worktree — refinar prosa para discriminar caso (mensagem do backlog específica nomeando o worktree onde a branch está) + reforçar "informar e parar, não improvisar `git checkout` no working tree principal" no `## O que NÃO fazer`. Gap detectado em sessão real do consumer `connector-pje-mandamus-tjpa`: agente leu lista parentética como exaustiva e improvisou `git checkout master` sem `git status` prévio (ação de blast-radius compartilhado). ADR-028 § Mecânica sincronizada in-place (status Proposto autoriza); 4 categorias (a) inexistente / (b) já checked out / (c) `.worktrees/<slug>/` órfão / (d) outras falhas com mensagens específicas por categoria. 2 findings do design-reviewer absorvidos no plano (bullet do `## O que NÃO fazer` lidera com critério; granularidade `<path>` rejeitada por YAGNI); decisões via cutucada do `/triage` para sub-bullet (d), YAGNI no cenário (c) da validação manual, e refinar ADR-028 in-place. Validação manual: cenários (a) regressão e (b) principal exercitados em consumer (operador confirmou no gate `Validei agora`).
- plugin: `/init-config` aceita `CLAUDE.md` gitignored e replica via `.worktreeinclude` (ADR-030) — sucessor parcial da extrapolação informal de ADR-016 em `/init-config` step 3 (que recusava com mensagem doutrinária); step 3 reformulado para registrar flag interna e prosseguir, step 4.5 estendido com cláusula OR no critério (≥1 role local OR `claude_md_gitignored = true`) e tabela de paths a garantir (`.claude/` per ADR-018, `CLAUDE.md` per ADR-030; cada adição independente e idempotente), step 5 emite linha de aceitação substituindo a mensagem revogada, `## O que NÃO fazer` ganha bullet documentando a guarda doutrinária inversa. `docs/install.md` linha 70 reformulada refletindo critério OR. ADR-029 § Limitações revisada via cutucada do design-reviewer (operador escolheu reabrir editorialmente vs reconhecer em ADR-030). 4 findings do design-reviewer absorvidos no ADR + 3 no plano + 0 no Bloco 1 (code-reviewer veredict: pronto). Validação manual (7 cenários cobrindo primeira invocação, role local concorrente, regressão canonical, idempotência, política mid-stream, worktree replication, upgrade) exercitada mentalmente sobre o diff; smoke real fica como spec pós-release.
- plugin: ADR-029 + cutucada cobre `CLAUDE.md` ausente — sucessor parcial de ADR-017 § Limitações; string-B nova emitida pelas 5 skills com `roles.required` quando `CLAUDE.md` ausente; string-A preservada; dedup conversation-scoped por string; CLAUDE.md + 5 SKILLs tocadas (`/init-config` inalterada).
- plugin: Skill `/draft-idea` para elicitação estruturada de `IDEA.md` (papel `product_direction`) — produtora do papel no toolkit, upstream de `/triage`. Interview multi-turn cobrindo problema, persona/usuário, restrições (enum), critérios de sucesso, alternativas descartadas. Probe canonical + dual: ausente → one-shot full; presente → update seção-a-seção via enum multi-select. Decisões em [ADR-027](docs/decisions/ADR-027-skill-draft-idea-elicitacao-product-direction.md): naming alinha ao filename canonical (paralelo a /new-adr ↔ ADR-NNN.md), sugere /triage como próximo passo, stack-agnóstica per ADR-008, `disable-model-invocation: false` per ADR-023, variante da track "Oferecer criação canonical via enum" do ADR-003 (presença determina modo, não cancela operação). Findings do design-reviewer e doc-reviewer absorvidos pré-commit per ADR-026; bloco extra do gate sanity check atualiza tabela "What's inside" do README.md e smoke list em docs/install.md. Validação manual (5 cenários: one-shot, update, Other em one-shot, cutucada descoberta, update + Other) fica como spec para smoke pós-release em consumer project.
- plugin: critério mecânico para absorção de findings do design-reviewer pré-commit — sucessor parcial de ADR-011 § Decisão #1; default invertido (absorver) + 3 condições de cutucada; commit message ganha seção estruturada `## design-reviewer findings absorvidos` para auditoria pós-fato.
- plugin: recusar cross-mode `backlog: local + plans_dir: canonical` no `/init-config` — per [ADR-025](docs/decisions/ADR-025-recusar-cross-mode-backlog-local-init-config.md), sucessor parcial de [ADR-005](docs/decisions/ADR-005-modo-local-gitignored-roles.md). Combinação semanticamente incoerente (texto privado vaza para plano público via `**Linha do backlog:**`) recusada antes de gravar bloco YAML; mensagem orienta para 3 combinações suportadas (ambos canonical / ambos local / backlog canonical + plans local). Recusa assimétrica — combinação simétrica permanece válida ("direção do leak" é o critério). Defensividade assimétrica em `/triage` step 1 cobre bloco legacy (opção (b) sobre 3 alternativas pós-finding alto do design-reviewer no ADR draft); `/run-plan` sem check (plano fora-do-/triage é fora-do-escopo). 11 findings absorvidos pré-commit do ADR + plano (precisão de origem, forma do cross-ref, verbalização do critério, defensividade legacy YAGNI-aware, cross-ref ADR-016 imprecisa, texto literal vs adaptado vocativamente, bullet do `## O que NÃO fazer` reorientado para tentação editorial, rodapé do sub-fluxo /triage step 4, grep miscalibrado, cenário manual extra, path do roadmap). H_arch da Onda 4 do roadmap 2026-05-12 — reaberto pós-diferimento ("só com evidência empírica de uso não-intencional") com trade-off de YAGNI absorvido pelo operador. ADR-005 § Limitações ganha cross-ref textual; Status preservado em `Proposto`. ~10 linhas de prosa edge removidas cross-skill + ~3 linhas de check defensivo adicionadas em `/triage` step 1 = saldo líquido ~7 linhas. Validação manual (3 cenários: Recusa via bloco ausente / Recusa via Editar bloco existente / Aceitação simétrica / Legacy) pendente para smoke-test em consumer real pós-release.
- plugin: `/triage` consolidação do backlog reposicionada como sub-fluxo do passo 4 — anatômica: parte da gravação do BACKLOG, não fase própria. Conteúdo do passo 5 anterior (releitura → flag duplicata/obsolescência → skip silente sem flags / enum único `Backlog` com flags) absorvido após a subseção "BACKLOG" do passo 4; passo 6 renumerado para 5. Anatomia visível cai de 7 passos (0-6) para 6 (0-5); comportamento idêntico. Cross-refs em `/run-plan §3.5` ("algoritmo completo em /triage SKILL → passo 4 sub-fluxo Consolidação") e `/new-adr` passo 5 ("/triage passo 5 reconhece que /new-adr já cobriu") atualizadas. F_arch da Onda 4 do roadmap 2026-05-12. Doc-reviewer flagou 2 drifts cross-SKILL pré-commit; corrigidos.
- plugin: `qa-reviewer` referencia sub-blocos canonical de `/gen-tests` por stack — per [ADR-019](docs/decisions/ADR-019-qa-reviewer-referencia-sub-blocos-gen-tests.md). Nova seção `## Idioms canonical por stack` em `agents/qa-reviewer.md` aponta `pyproject.toml` → Python (`respx`, `tmp_path`, `pytest-asyncio`) e `pom.xml`/`build.gradle*` → JVM (JUnit 5, Mockito, layout Maven). Carregamento lazy (sub-bloco lido só se diff toca paths de teste); stack sem sub-bloco → prossegue com regras conceituais cross-stack e cita ausência no relatório. Convergência conceitual com pipeline `ubiquitous_language` → reviewer (`philosophy.md:75`), divergindo na forma (carrier dinâmico no plano vs lookup estático sob demanda). Alternativa de dispatch `/run-plan` → `/gen-tests` rejeitada (mismatch de argumentos plano→alvo, escopo de bloco misto prod+test, fragmentação da continuidade com cutucadas internas de gen-tests, perda do contexto do agente implementador).
- plugin: anti-patterns de mocking adicionados em `agents/qa-reviewer.md` item 4 "Mock vs real" — 3 sub-bullets novos (Testing mock behavior, Test-only methods, Mocking without understanding) cobrindo lentes (1)/(2)/(3) de `testing-anti-patterns.md` (obra/superpowers). Cada sub-bullet traz sintoma concreto identificável pelo reviewer no diff (assert sobre `mock.return_value`/`mock.call_args` em vez do efeito observável; métodos `_set_state_for_test()` ou getters de estado interno usados só em teste; mock com retorno/side-effect/ordem divergente do contrato real). Lentes (4) "incomplete mocks" e (5) "integration as afterthought" não tocadas (já parcialmente cobertas). Reviewer permanece compacto.
- plugin: `/init-config` passo 4.5 garante `.claude/` em `.worktreeinclude` quando ≥1 role em modo local (per [ADR-018](docs/decisions/ADR-018-replicacao-claude-em-modo-local-init-config.md)). Mecânica determinística sem `AskUserQuestion`: ausente → criar com header + `.claude/`; sem `.claude/` → adicionar linha; com `.claude/` → skip silente (idempotente). Compatível com `.worktreeinclude` tracked ou gitignored. `/run-plan` SKILL.md:36 preservado como safety net. ADR-005:48 e :75 reformulados referenciando ADR-018. `docs/install.md` ganha nota sobre tracked/gitignored + comportamento canonical. Origem: smoke-test PJe 2026-05-11. Validação manual: 11 cenários no plano para smoke pós-release.
- plugin: skill `/init-config` + cutucada uniforme de descoberta nas 4 skills com `roles.required` (`/triage`, `/new-adr`, `/run-plan`, `/next`). Wizard interativo pergunta `decisions_dir`, `backlog`, `plans_dir` (canonical/local/null) + `test_command` (probe stack-aware: Maven, pytest/uv, npm, make) e grava o bloco `<!-- pragmatic-toolkit:config -->` no `CLAUDE.md`. Sub-caso `CLAUDE.md` gitignored para com mensagem alinhada com ADR-016 (orienta reconsiderar `.gitignore`, sem prescrever workaround). Política de cutucada uniforme registrada em [ADR-017](docs/decisions/ADR-017-cutucada-uniforme-descoberta-config-ausente.md): gating triplo (existe + marker ausente + dedup conversation-scoped via leitura do contexto visível), redação canonical fixa, herança editorial. Seção `## Cutucada de descoberta` adicionada ao `CLAUDE.md` do toolkit. `docs/install.md` introduz `/init-config` como caminho recomendado. Validação manual: 18 cenários enumerados no plano `docs/plans/init-config-wizard.md`; ficam como spec para smoke pós-release (operador rodará no PJe após reinstall).
- plugin: sub-bloco Java/Maven em `skills/gen-tests/SKILL.md` — primeira aplicação a uma segunda stack desde [ADR-008](docs/decisions/ADR-008-skills-geradoras-stack-agnosticas.md). JUnit 5 + Mockito + layout Maven `<módulo>/src/test/java/<package>/` + comando de validação `mvn -pl <módulo> test -Dtest=<NomeClasse> -DfailIfNoTests=false`. Entry `pom.xml → Java` na tabela marker→stack; frontmatter `description` atualizada; fallback de "Marker ausente" lista `Python, Java`. JUnit 5 only na v1 (JUnit 4 gatilho de revisão). Spring/Spring Boot fora de escopo (sub-bloco futuro se demandar). Validação manual pendente: invocar `/gen-tests` no PJe após Frente B avançar e confirmar detecção via `pom.xml` + geração coerente com idioms.
- plugin: política do `block_gitignored.py` para scripts operacionais (achado #12 fase 2 do smoke-test PJe) — avaliado e descartado per [ADR-016](docs/decisions/ADR-016-manter-block-gitignored-scripts-no-consumer.md): hook fica como está; 7 alternativas (a–f, e′) descartadas por fricção doutrinária (Path contract + hooks sem flags) ou operacional; pattern do consumer (script gitignored como entrypoint de workflow) resolve-se via Makefile/Dockerfile/compose com parametrização por dev, fora da blast radius do plugin. Gatilhos de revisão concretos no ADR (≥2 consumers reportando + impossibilidade de refatoração; incidente concreto de bug por workaround manual; mudança na doutrina de naming/auto-gating; mudança de convenção de ecossistema).
- plugin: fix do `block_env.py` per [ADR-015](docs/decisions/ADR-015-bloquear-env-files-por-sufixo.md) — predicado por sufixo `.env` em vez de dotfile-strict, espelha exceção `*.env.example` para qualquer prefixo, preserva strip de `TEMPLATE_SUFFIXES` mantendo defensividade para templates do env-principal. Validação manual: 20 fixtures (status quo + cobertura nova + templates defensivos + examples + não-env). Origem: smoke-test PJe (achado #1 fase 1).
- plugin: marketplace prep #8 (estrutural-grande) — avaliado e descartado per [ADR-014](docs/decisions/ADR-014-inventario-editorial-main-unico.md): refatoração (branch dev, repo separado, modo local, orphan publish) tem custo desproporcional ao benefício; clone do consumer arrasta `git history` completo de qualquer jeito (só `filter-repo` destrutivo resolveria pack size); transparência editorial pública tem valor positivo (visitor vê o workflow funcionando). Mantém `main` único. Gatilhos de revisão concretos no ADR (atrito reportado, inventário ≥10 MB, etc.).
- plugin: marketplace prep #6 (infra) — CI mínimo de validação de manifests via GitHub Action (~20 linhas YAML): `python -m json.tool` em `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `hooks/hooks.json`; `python -c "import ast; ast.parse(open(p).read())"` em cada `hooks/*.py`. Fecha classe inteira de "release quebrada por typo" sem suite de testes.
- plugin: marketplace prep batch 1 (itens #1-#5) — saneamento `marketplace.json` (#1: removido `$schema` 404, `description` movida para `metadata.description` per warning do `claude plugin validate`), `docs/audits/runs/README.md` criado (#2), `docs/install.md` cita `claude plugin validate` como ferramenta oficial e documenta Python 3.10+ (#3 + #4), keywords/tags + descriptions realinhadas (#5: removidos `python`/`pytest`/`testing`/`scaffold`/`diagnosis`; adicionados `decision-records`/`doc-review`/`design-review`/`worktree`/`self-gated-hooks`; descriptions de ambos manifests trocam `Python testing scaffolder` por `stack-aware test scaffolder (Python today)` e atualizam lista de reviewers para incluir doc/design).
- plugin: README em EN para discoverability no Claude Code marketplace + ADR-012 codificando inversão parcial da doutrina de idioma de docs
- plugin: hook `block_gitignored` correção pós-shipping — (a) allowlist de `<repo>/.claude/` (modo local-gitignored do ADR-005 estava sendo bloqueado em escritas a `.claude/local/<role>/`, quebrando `/triage`, `/new-adr` e `/run-plan` em consumers que adotaram `paths.<role>: local`); (b) ancoragem em primeiro ancestral existente do `file_path` (regressão silenciosa onde `git -C <pai-inexistente>` falhava e o hook liberava o edit em vez de bloquear). Smoke em 9 cenários cobrindo allowlist, regressão de bloqueio, paths tracked, fora-de-git, prefixo similar não-allowlisted (`.claudefoo/`), payloads degenerados.
- plugin: hook `block_gitignored` — PreToolUse (Edit|Write) que bloqueia edits em paths cobertos pelo `.gitignore` do consumer (ex.: `.venv/`, `node_modules/`, `dist/`, `target/`, `.cache/`). Fecha modo de falha real e cross-stack onde o modelo tenta "consertar" código dentro de dependência ou artefato de build em vez de ajustar fontes do projeto. Auto-gating triplo (file_path vazio / não-git / `git` ausente → exit 0) mantém silêncio fora de escopo. Sinal vem de intenção declarada do repo (`.gitignore`), não de heurística codificada — robusto a stacks novas sem alterar o hook. Cinco cenários smoke-testados (path tracked, path ignored, fora de git, file_path vazio, JSON inválido).
- plugin: `/triage` step 4 — chamar `Skill(name='pragmatic-dev-toolkit:new-adr', args=<título>)` em vez de prosa "invocar /new-adr" — reabilita parte 1 do Bloco 5 do plano `instrumentar-skills-multi-passo.md` (PR #44, revertida por generalização errada do code-reviewer entre cenário /run-plan worktree e /triage no main). Diagnóstico `/debug` 2026-05-08 confirmou: cenário do Bloco 1 (Skill em /run-plan worktree → ADR no main) é distinto do cenário do Bloco 5 (Skill em /triage no main → ADR no main, correto). Ganho: determinismo do step 4 (modelo às vezes ignora skill dedicada e cria ADR direto via Write, perdendo numeração e wiring do design-reviewer per ADR-011). Mudança trivial — 1 frase em `skills/triage/SKILL.md` step 4 + ajuste editorial em `docs/plans/instrumentar-skills-multi-passo.md` `## Pendências de validação` marcando parte 1 como fechada (parte 2 sobre paralelismo continua YAGNI).
- plugin: `/next` varrer também `## Pendências de validação` em planos `<plans_dir>/*.md` — hoje só lê BACKLOG, mas pendências de validação são fonte paralela de pendentes (memory `feedback_backlog_scope`: pendência fica no plano, não no backlog) sem mecanismo de descoberta. Direção: listagem separada (rationale é fechamento de plano específico, não estratégia × amplitude do top 3); filtrar planos em curso (worktree ativa / PR aberto). Caso real: pendências (a) e (b) do plano `instrumentar-skills-multi-passo.md` invisíveis ao `/next` até operador trazer manualmente.
- plugin: wiring automático do `design-reviewer` — após dogfood acumular evidência, decidir se vira gate em `/run-plan` pré-loop (revisão obrigatória do plano antes do loop de blocos) e/ou em `/new-adr` pré-commit (revisão do ADR draft). Reavaliar trade-off de tokens em alta frequência (free-read de `docs/decisions/` + `philosophy.md` por invocação). Captura prevista no plano `docs/plans/agent-design-reviewer.md`.
- plugin: `/run-plan` §3.3 (sanity check de docs user-facing) — fazer grep dos identificadores tocados nos paths user-facing (README, install, guides) **antes** de cutucar. Grep vazio → seguir direto sem perguntar (skip silente justificado empiricamente, terceira condição de skip além das duas atuais). Grep com matches → cutucar listando os referrers concretos com path:linha, não candidatos genéricos. Cutucada com palpite ("candidatos típicos: README, install") ou cutucada vazia ("verifiquei e não achei nada, segue?") não cumpre o trabalho que a regra prescreve ("citar antes a superfície inferida e os candidatos típicos como prosa informativa") — a "prosa informativa" carrega evidência concreta ou fundamenta o skip. Flagado em uso real durante `/run-plan triage-step0-already-deleted-remote`.
- `/triage` step 0 cleanup pós-merge: tratar `git push origin --delete <slug>` falhando com `branch not found` / `remote ref does not exist` como sucesso silente + `git fetch --prune` para reconciliar refs locais órfãos. Sintoma observado no 1º dogfood real (pós-PR #44): GitHub auto-delete branches já havia removido a branch remota antes do step 0 rodar; spec atual reportou erro literal e parou, deixando refs órfãos pro operador limpar manual. Outras falhas de `push --delete` (network, perm) continuam reportando literal e parando — discriminação por matching de stderr.
- plugin: instrumentar skills multi-passo via tools do harness — TaskCreate em `/run-plan` loop e `/debug` ledger; Monitor para `test_command` longo; explicitar paralelismo em `/run-plan` §0 e §2.3 e Skill tool em `/triage`; convenção em CLAUDE.md.
- plugin: cutucada pós-merge para cleanup de worktree/branch local — `/run-plan` termina na worktree na branch da feature após oferecer Push/Push+PR (skill, linha final do passo 7) e não há retorno após o merge; `git worktree remove .worktrees/<slug>`, `git branch -d <slug>` e `git fetch --prune` ficam manuais. Direção possível: skill nova (`/post-merge-cleanup`) ou passo opcional anexado à próxima invocação de `/triage`/`/release` quando detectar worktrees em `.worktrees/` cuja branch já mergeou em `origin/<main>` (`git branch -r --merged origin/<main>` cruzado com `git worktree list`). Reavaliar se YAGNI: cleanup é trivial e o operador pode preferir manter a worktree para inspeção pós-merge — registrar para reavaliar se atrito recorrer.
- plugin: agent design-reviewer — revisor pré-fato de decisões arquiteturais/design em planos e ADRs; free-read de docs/decisions e philosophy.md; invocação manual via @-mention; wiring automático deferido após dogfood.
- plugin: skills geradoras stack-agnósticas via dispatch interno — inverter sufixo de stack na "Convenção de naming" (philosophy.md/CLAUDE.md); idioms vivem em sub-blocos do SKILL.md. `gen-tests-python` vira `gen-tests`. Hooks ficam fora. Implementa ADR-008.
- /run-plan: 5º warning pré-loop — plano altera código de produção sem listar arquivo de teste em `## Arquivos a alterar` → capturar como Validação ("cenário sem cobertura nova exercitada"). Análogo aos 4 atuais (alinhamento dirty, `.worktreeinclude`, credencial, escopo divergente); ADR-002 prevê 5º como gatilho de revisão. Heurística stack-agnóstica via patterns (`tests/`, `test_*`, `*_test.*`, `*.test.*`, `*.spec.*`, `__tests__/`, `src/test/`). Não gera teste — apenas cutuca via `## Pendências de validação`. Reavaliar se YAGNI: `/triage` step 2 já tem "Cobertura de teste?" no checklist; warning é belt-and-suspenders quando autor do plano ignora.
- plugin: `/release` compor mensagem de tag com síntese das mudanças — em vez de `Release vX.Y.Z` fixo, `git tag -a -m` carrega resumo compacto dos headers do CHANGELOG (Added/Changed/Fixed em 1-3 linhas). Útil para archeology offline (`git tag -n3`, `git show <tag>`) onde CHANGELOG não está acessível. Trade-off: duplica conteúdo já versionado; reavaliar se uso real em archeology justificar o custo.
- plugin: convenção que CHANGELOG.md e mensagens de tag anotada seguem idioma dos commits (ADR-007); migração retroativa do CHANGELOG histórico de PT para EN
- plugin: aplicar regra enum-first em SKILLs — converter prosa-com-bifurcação em AskUserQuestion (single/multi + Other auto + Recommended), unificar perguntas relacionadas, eliminar óbvias
- plugin: elevar convenção `## Implementação` em ADRs — `/new-adr` template ganha seção opcional listando commits implementadores; útil principalmente em modo local onde regra de não-referenciar (ADR-005) impede caminho inverso commit → ADR; em modo canonical é redundante com `git log --grep` mas pode destacar a sequência de implementação. Aplicada manualmente em ADR-005; considerar retrofit dos ADRs 001-004.
- plugin: reavaliar contrato `required` por role individualmente — `decisions_dir` e `backlog` têm semântica de artefato compartilhado (gitignore mata o ponto do ADR e do registro editorial), mas `plans_dir` faz sentido como artefato local-gitignored opcional. Não tratar como bloco único; cada role demanda análise distinta de fallback.
- /triage passo 2: heurística de ADR-worthy não cobre inversão de premissa arquitetural documentada nem captura de restrição externa duradoura — observado em uso real: triagem fechou em plano (sem ADR) para mudança que qualificava como política do sistema. Direção: enriquecer critério do passo 2 com dois gatilhos explícitos (a) mudança contradiz/inverte decisão registrada em `decisions_dir`; (b) mudança codifica restrição externa de longa duração (regulatória, contratual, integração estável). Reavaliar se merece sub-fluxo dedicado ou só nota editorial no passo.
- plugin: implementar auto-detect de forge em `/run-plan` (4.7) e `/release` — parse `git remote -v` → mapear domínio (`github.com` → `gh`, `gitlab.*` → `glab`) → executar comando do forge com gate de confirmação; fallback textual quando CLI ausente ou remote desconhecido. Continuação do item "desacoplar de GitHub-específico" em ## Concluídos.
- plugin: trocar `disable-model-invocation: true` para `false` em `/release`, `/run-plan` e `/new-adr` — blast radius real é baixo (release é local até push manual; run-plan opera em worktree isolada; new-adr só cria markdown), e inconveniência de não permitir autoinvocação supera o ganho do flag.
- /run-plan 3.4 sob D2/ADR-004: linha pré-existente em `## Próximos` + transição final que apenas **adiciona** em `## Concluídos` produz duplicata pós-merge. Fix: 3.4 volta a **mover** (remover de Próximos via matching texto exato com `**Linha do backlog:**` + adicionar em Concluídos). Linha não localizada em Próximos → fallback ao comportamento atual (só adicionar). Gap detectado durante /triage do handoff /debug → /triage.
- /debug → /triage: handoff perde contexto em sessão longa — /debug produz diagnóstico (incluindo ledger de hipóteses) em conversa que cai do contexto antes do operador invocar /triage. Direção possível: /debug passo 6 grava sumário em `.cache/debug-<timestamp>.md` (gitignored) que /triage lê se existir e recente (<24h); ou enriquecer a sugestão final do /debug com snippet pronto de invocação /triage. YAGNI até o pain ser reportado em uso real — registrar para reavaliar. Flagado na revisão arquitetural pós-v1.20.0.
- plugin: BACKLOG.md como state-tracker é fonte recorrente de merge artifact — 5 mecanismos defensivos (`/triage` push pós-commit, `/run-plan` precondição 2, `/run-plan` 3.7 auto-rebase, Action `validate-backlog`, `/heal-backlog`) protegem o mesmo problema estrutural: o arquivo mistura curadoria (Próximos) com state-tracking (Em andamento, Concluídos), e dois PRs concorrentes mutam ambos. Reavaliar mover state para git/forge (PR aberto = em andamento, mergeado = concluído) — 4 dos 5 mecanismos viram desnecessários. Mudança grande, exige ADR antes de implementar. Flagado na revisão arquitetural pós-v1.20.0.
- plugin: batch 3/B2 — frontmatter declarativo `roles:` nas SKILLs (implementa ADR-003), CLAUDE.md absorve regra de despacho automático, skills removem prosa de Pré-condições enumerando papéis
- plugin: batch 3/C1 — eliminar gates de cutucada na fase pré-loop do /run-plan (implementa ADR-002), capturar warnings via trilhos existentes do passo 3.5
- plugin: batch 3/E3 — extrair `templates/plan.md` centralizado e atualizar /triage e /run-plan para apontar ao template (implementa ADR-001)
- plugin: batch 2 de refactor de fronteira documental — A1 desduplica papéis philosophy.md ↔ CLAUDE.md, A2 continua mecânica residual de philosophy.md para consumers, B1 declara aplicabilidade condicional em code-reviewer settings.json/infra
- /triage e /run-plan: redundância na descrição do reviewer dispatch — /triage descreve o comportamento do /run-plan ("/run-plan invoca o agent correspondente") em vez de focar só na sintaxe da anotação `{reviewer: ...}` no plano. Flagado pelo `code-reviewer` no Bloco 4 do plano `batch1-tightening-editorial` como finding fora-do-escopo.
- plugin: batch 1 de tightening editorial pós-v1.20.0 — sanity check de docs como prosa (/run-plan 4.3), trim disciplinado de `## O que NÃO fazer` + critério editorial em CLAUDE.md, `disable-model-invocation` em /release e /run-plan, single-reviewer como caso normal (multi como exceção), /next propõe commit das movimentações automáticas
- plugin: desacoplar de GitHub-específico — `/run-plan` (4.7) chama `gh pr create` no enum `Push + abrir PR`, e `/release` sugere `gh release create` no texto final; consumidores em GitLab (corporativo) ficam sem caminho. Direção mínima: substituir chamada e texto por sugestão neutra ao operador (push + instrução textual com exemplos `gh`/`glab`/UI web), sem auto-detect nem role `forge`. Reavaliar evolução (auto-detect via `git remote -v` ou role `forge` no path contract) só se atrito recorrente justificar a abstração.
- CLAUDE.md: ajustar frase "Skills/agents end with an explicit `## O que NÃO fazer` section" — agents reviewer (code/qa/security/doc) não têm essa seção; padrão se aplica apenas a skills. Flagado pelo `code-reviewer` durante execução do plano `agent-doc-reviewer-drift`.
- agent doc-reviewer: revisor genérico de drift entre doc e código (identificadores, cross-refs, exemplos) — quarto reviewer ao lado de code/qa/security; cross-cutting (código+doc); default em blocos .md-only no /run-plan.
- /release: após `git checkout <branch-da-pré-condição-2>` no recovery proativo do Aplicar, auto-sync com upstream (`git pull --ff-only` se HEAD atrás do remote) antes da sequência (a)-(e) — evita taggear SHA atrasado em janelas concorrentes onde merge/push remoto aconteceu durante prep da release. Flagado pelo `code-reviewer` como gap de design durante execução do plano `release-head-check-and-action-dedup`.
- /release: verificar HEAD branch (não detached, mesma branch da pré-condição 2) imediatamente antes do `git commit` no passo 4.Aplicar — guarda contra mudança de HEAD por sessão concorrente em outro terminal. Sintoma observado na release v1.18.0: `git checkout v1.17.0` rodado em paralelo detachou HEAD; commit nasceu em linha paralela com tree de v1.17.0 + version bumps, fora do main.
- Action `validate-backlog`: deduplicar issues abertas com mesma label antes de criar nova (`gh issue list --label backlog-merge-artifact --state open`) — evita N issues duplicadas em pushes sucessivos com artefato. Flagado pelo `security-reviewer` como editorial (fora-de-escopo de segurança) durante execução do plano `backlog-validate-and-heal`.
- /run-plan 4.7: auto-rebase pré-push não cobre fan-out de PRs — quando 2 PRs ficam abertos em paralelo e um merge muda BACKLOG.md em main, o segundo PR pode mergear com merge artifact (linha duplicada em Em andamento+Concluídos ou linha sumida). Ocorreu na 3ª vez ao mergear PR #20 + #21. Considerar: detecção pós-merge no main + script de cura, ou bloqueio de /triage quando há PR aberto que ainda não fundiu.
- philosophy.md: refatorar para conter apenas princípios; migrar mecânicas para os consumidores reais (skills/CLAUDE.md/README)
- Skills: compactar prosa e enxugar `## O que NÃO fazer` para reduzir input recorrente
- CLAUDE.md: cortar paráfrases de philosophy.md/skills/agents para reduzir input tokens auto-loaded a cada interação
- /run-plan: conflito recorrente em BACKLOG.md ao fundir PR — transição final (Em andamento → Concluídos) conflita com estado divergente de main; ocorreu três vezes; investigar se guarda pré-condição 2b cobre todos os cenários
- /run-plan: captura automática de bloqueios em pré-condição de worktree (fase anterior ao loop de arquivos)
- /triage: push pós-commit determinístico — caminho-com-plano consolida commit + push num único shell call (gate Commit cobre a unidade)
- /run-plan: sugerir push e abertura de PR ao final, além do commit (quando aplicável)
- /run-plan + philosophy.md: distinguir capturas de validação (plano) de capturas de feature/correção (backlog) na regra de captura automática

- /triage + /run-plan: merge artifact eliminado — triage empurra main ao remote após commit; run-plan bloqueia se main estiver à frente do remote com Em andamento no backlog
- /triage: guarda explícita contra inclusão de BACKLOG.md em ## Arquivos a alterar — transições gerenciadas pelo campo Linha do backlog
- /run-plan: condição de skip do sanity check de docs (4.3) restrita a padrões user-facing (positive list: README*, CHANGELOG*, install.md, docs/guides/**)
- skill /next: orientação de sessão — analisa backlog, verifica implementados e sugere top 3 por impacto; /triage delega a /next quando invocada sem argumento
- /run-plan: refinar sanity check de documentação no passo 4.3
- release: colapsar gates de version_files, changelog e commit/tag num único review final consolidado
- captura automática de imprevistos durante /run-plan e validação manual
- backlog: transições de estado automáticas sem cutucada ao operador
