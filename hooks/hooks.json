{
  "description": "Block direct edits to .env files; run pytest after Python edits in projects with a pyproject.toml ancestor.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "description": "Timeout 10s — block_env is a fast filename-only triage; anything slower is a bug.",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/block_env.py",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "description": "Timeout 60s — fits a typical suite; slower projects let the hook expire silently (PostToolUse must exit 0).",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/run_pytest_python.py",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
